# Valkey 캐시 — 순수 매니페스트 소스 (ch6.3, helm 제거).
# GSM 파일을 직접 읽는 StatefulSet/Service/SecretProviderClass를 k8s/valkey/manifests에 둔다.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: notiflex-valkey
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/jinnypark9393/notiflex-platform-test.git
    targetRevision: main
    path: k8s/valkey/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: notiflex
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
