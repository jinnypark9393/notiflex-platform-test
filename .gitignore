# macOS
.DS_Store

# 에디터
.idea/
.vscode/
*.swp

# Terraform
# 참고: .terraform.lock.hcl은 provider 버전 고정을 위해 커밋한다 (제외하지 않음).
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# direnv (로컬 환경변수 — 커밋 금지)
.envrc
.direnv/
